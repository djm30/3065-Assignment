const validate = require("../../src/validate");

describe("Validates the query params to ensure a valid request has been sent", () => {
  test.skip("Succeeds with full array of modules and marks", () => {});
});
